import React from 'react';
import Nav from './Nav';
var Header;

export default Header = React.createClass({
  render() {
    return (
      <div>
        <Nav />
        <br />
      </div>
    );
  }
});
